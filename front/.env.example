# Backend API base URL.
# Leave empty in local dev — Vite proxies /api to localhost:3001.
# In production (Vercel), set this to your Render backend URL:
# VITE_API_URL=https://edmebot-api.onrender.com
VITE_API_URL=
